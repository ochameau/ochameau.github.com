### new blog post
cd ~/ochameau-*/source/_posts/

## first connect to sftp, do ssh to host
ssh vps141915.ovh.net

### upload
cd ~/ochameau-truc
jekyll
cd public/
lftp -e "mirror -R -n -x .git --ignore-time; quit" sftp://root@vps141915.ovh.net/var/www/

